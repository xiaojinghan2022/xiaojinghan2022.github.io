[YAML]
<proxy-groups>+:
  - name: Proxy
    type: select
    proxies:
      - Proxies
<proxy-groups>*:
  where:
    name: '/^(?!.*(?:HK|SG|JP|US|TW|KR|Proxy|Proxies)).+$/'
    type: select
  set:
    proxies:
      - Proxy